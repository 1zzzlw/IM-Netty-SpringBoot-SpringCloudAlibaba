<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzzlew.zzzimserver.mapper.GroupConversationMapper">

    <insert id="insertGroupConversation"
            parameterType="com.zzzlew.zzzimserver.pojo.dto.conversation.GroupConversationDTO">
        insert into group_conversation (id, group_name, group_avatar, owner_id)
        values (#{id}, #{groupName}, #{groupAvatar}, #{ownerId})
    </insert>

    <insert id="insertGroupMember"
            parameterType="com.zzzlew.zzzimserver.pojo.dto.user.GroupMemberDTO">
        insert into group_member (group_id, user_id, role)
        values (#{groupId}, #{userId}, #{role})
    </insert>

    <update id="updateGroupMemberCount">
        update group_conversation
        set member_count = member_count + 1, group_avatar = #{groupAvatar}
        where id = #{conversationId}
    </update>

    <update id="updateConversationStatus">
        UPDATE group_member
        SET latest_msg_time = #{sendTime},
            latest_msg      = #{content},
            status          = 1,
            unread_count    = CASE
                                  WHEN user_id != #{userId} THEN unread_count + 1
                                  ELSE unread_count
                END
        WHERE group_id = #{conversationId};
    </update>

    <select id="selectGroupConversationListByConversationIdList"
            resultType="com.zzzlew.zzzimserver.pojo.entity.GroupConversation">
        select id, group_name, group_avatar, member_count
        from group_conversation
        where id in
        <foreach collection="groupIdList" item="groupId" open="(" close=")" separator=",">
            #{groupId}
        </foreach>
    </select>
</mapper>